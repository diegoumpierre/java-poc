--liquibase formatted sql

--changeset chat:data-001-1-migrate-users preconditions-sql-check
--preconditions onFail:MARK_RAN
--precondition-sql-check expectedResult:1 SELECT COUNT(*) FROM information_schema.TABLES WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'NOTF_CHAT_USER'
INSERT INTO CHAT_USER (ID, EXTERNAL_USER_ID, TENANT_ID, NAME, EMAIL, AVATAR_URL, STATUS, LAST_SEEN_AT, CREATED_AT, UPDATED_AT)
SELECT ID, EXTERNAL_USER_ID, TENANT_ID, NAME, EMAIL, AVATAR_URL, STATUS, LAST_SEEN_AT, CREATED_AT, UPDATED_AT
FROM NOTF_CHAT_USER
WHERE NOT EXISTS (SELECT 1 FROM CHAT_USER WHERE CHAT_USER.ID = NOTF_CHAT_USER.ID);

--changeset chat:data-001-2-migrate-conversations preconditions-sql-check
--preconditions onFail:MARK_RAN
--precondition-sql-check expectedResult:1 SELECT COUNT(*) FROM information_schema.TABLES WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'NOTF_CHAT_CONVERSATION'
INSERT INTO CHAT_CONVERSATION (ID, TENANT_ID, PARTICIPANT_ONE_ID, PARTICIPANT_TWO_ID, LAST_MESSAGE_AT, CREATED_AT, UPDATED_AT)
SELECT ID, TENANT_ID, PARTICIPANT_ONE_ID, PARTICIPANT_TWO_ID, LAST_MESSAGE_AT, CREATED_AT, UPDATED_AT
FROM NOTF_CHAT_CONVERSATION
WHERE NOT EXISTS (SELECT 1 FROM CHAT_CONVERSATION WHERE CHAT_CONVERSATION.ID = NOTF_CHAT_CONVERSATION.ID);

--changeset chat:data-001-3-migrate-messages preconditions-sql-check
--preconditions onFail:MARK_RAN
--precondition-sql-check expectedResult:1 SELECT COUNT(*) FROM information_schema.TABLES WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'NOTF_CHAT_MESSAGE'
INSERT INTO CHAT_MESSAGE (ID, CONVERSATION_ID, SENDER_ID, TEXT, READ_AT, CREATED_AT, UPDATED_AT)
SELECT ID, CONVERSATION_ID, SENDER_ID, TEXT, READ_AT, CREATED_AT, UPDATED_AT
FROM NOTF_CHAT_MESSAGE
WHERE NOT EXISTS (SELECT 1 FROM CHAT_MESSAGE WHERE CHAT_MESSAGE.ID = NOTF_CHAT_MESSAGE.ID);
