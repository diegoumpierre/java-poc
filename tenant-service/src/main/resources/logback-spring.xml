<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="tenant-service"/>
    <springProperty scope="context" name="lokiUrl" source="loki.url" defaultValue="http://localhost:3100/loki/api/v1/push"/>

    <!--
    MDC Keys available in all logs:
    - correlationId: Distributed tracing ID across services
    - requestId: Unique ID for this specific request
    - tenantId: Tenant ID for multi-tenant segregation
    - userId: User ID for user activity tracking
    - clientIp: Client IP address
    - traceId/spanId: OpenTelemetry tracing (if enabled)
    -->

    <!-- Console Appender with full trace context including tenant/user -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %5p [${appName}] [tenant:%X{tenantId:-anonymous}] [user:%X{userId:-anonymous}] [corr:%X{correlationId:-}] [ip:%X{clientIp:-}] [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Loki Appender (only active with observability profile) -->
    <springProfile name="observability">
        <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
            <http>
                <url>${lokiUrl}</url>
            </http>
            <format>
                <label>
                    <pattern>application=${appName},host=${HOSTNAME},level=%level,tenant=%X{tenantId:-anonymous}</pattern>
                </label>
                <message>
                    <pattern>{"timestamp":"%d{yyyy-MM-dd'T'HH:mm:ss.SSS'Z'}","level":"%level","logger":"%logger{36}","message":"%msg","correlationId":"%X{correlationId:-}","requestId":"%X{requestId:-}","tenantId":"%X{tenantId:-}","userId":"%X{userId:-}","clientIp":"%X{clientIp:-}","traceId":"%X{traceId:-}","spanId":"%X{spanId:-}"}</pattern>
                </message>
            </format>
        </appender>

        <root level="INFO">
            <appender-ref ref="LOKI"/>
        </root>
    </springProfile>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="com.poc" level="DEBUG"/>
</configuration>
